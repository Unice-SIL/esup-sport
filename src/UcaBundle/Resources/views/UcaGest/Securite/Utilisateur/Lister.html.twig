{% extends "@Uca/Common/Liste/Datatable.html.twig" %}

{% block DatatableHeader%}
    {% if not noAddButton is defined %}
        <div class="row">
            <div class="col-lg-6">
                <a href="{{ url('fos_user_registration_register') }}" class="btn btn-primary btn-xs">{{ 'bouton.ajouter'|trans }}</a>
            </div>
        </div>
    {% endif %}
{% endblock %}
