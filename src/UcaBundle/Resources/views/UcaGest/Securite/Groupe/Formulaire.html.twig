    {% include "@Uca/Common/Component/Bouton.Retour.html.twig" %}

    <h1>{{ titrePage | trans}}</h1>

    {% if not form.vars.valid %}
        <div class="alert alert-danger" role="alert" sr-only="">
            {{ 'erreur.formulaire.champ.incorrect' | trans }}
        </div>
    {% endif %}

    <div class="well">
    <div class="row">
        <div class="col-sm-12 col-lg-6"> 
        {{ form_start(form, { 'action': formPath, 'attr': { 'novalidate': 'novalidate' } }) }}
        {{ form_widget(form) }}
        {{ form_end(form) }}
        </div>
    </div>
    </div>
