{% extends "UserBundle/layout.html.twig" %}

{% block fos_user_content %}
    <a class="btn btn-primary" href="{{ path('UcaGest_UtilisateurLister') }}"><i class="fas fa-chevron-left"></i> Retour</a>
    <h1>{{'utilisateur.verification.title'|trans}}</h1>
    <p>{{ 'registration.check_email'|trans({'%email%': user.email}) }}</p>
{% endblock fos_user_content %}