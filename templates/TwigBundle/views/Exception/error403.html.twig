{# Template erreur 403 #}
{% set message = 'erreur.403.message' %}
{% if 'wrong' not in exception.message %}
    {% set message = exception.message %}
{% endif %}
{{ include("UcaBundle/Exception/error.html.twig", 
    {
        'textError': "erreur.403",
        'errorIcon': "erreur.403.icon",
        'errorMessage': message,
        'errorButton': "erreur.button"
    })
}}