<div aria-hidden="true" class="modal z-index-top fade" id="modalCGV" role="dialog" tabindex="-1">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header d-flex justify-content-center text-center">
                <h2 aria-labelledby="modalCGV" class="modal-title fs-24 hide-border-title mt-5 mt-md-0">
                    {{ 'cgv.modal.titre' | trans | upper }}
                </h2>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <div class="image-presentation" aria-hidden="true">
                        <svg width="41" height="41">
                            <use href="{{ asset('images/remove.svg') ~ '#remove' }}" ></use>
                        </svg>
                    </div>
                </button>
            </div>
            <div class="modal-body">
                <p>
                    {{ 'cgv.information' | trans }}
                </p>
            </div>
            <div class="modal-footer">
                <button class="btn btn-secondary" data-dismiss="modal" type="button">{{ 'common.fermer' | trans }}</button>
                <a href="{{ path('UcaWeb_CGV') }}" class="btn btn-primary js-bouton-action d-block" type="button">{{ 'bouton.voir' | trans }}</a>
            </div>
        </div>
    </div>
</div>
