{% for commandeDetail in row.commandeDetails %}
    {% if commandeDetail.typeArticle == "TypeAutorisation" or commandeDetail.type == 'autorisation' %}
        {% if isCarte(commandeDetail.id) %}
            {{ getInformationCarte(commandeDetail.id) }}
        {% endif %}
    {% endif %}    
{% endfor %}